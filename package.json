{
  "name": "chorouk-app",
  "description": "Chorouk application",
  "version": "1.0.0",
  "engines": {
    "node": "12.16.x"
  }
"scripts": {
    "install": "(cd ClientApp && npm i)",   
    "heroku-postbuild": "cd ClientApp && npm run build-prod && mv dist/frontend ../backend/",
    "start": "if [ \"$NODE_ENV\" == \"production\" ]; then cd ServerApp && python manage.py runserver; else cd ServerApp && python manage.py runserver & (cd ClientApp && npm start); fi"
}
}